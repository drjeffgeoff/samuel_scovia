#include <stdio.h>
#include <stdbool.h> //For boolean data types

// Function that takes integers a
// and returns a boolean

bool isEven(int num);
    if (num % 2 == 0) {
        return true; //If even
    }else {
        return false;  // If Odd
    }

    int main() {
        int number = 8;

        // Function call
        if (result) {
            printf("%d is even \n", number);
        }else {
            printf("%d is odd \n", number);
        }
        return 0;
    }

    // Course Work Practical
    