#include <stdio.h>

int main()
{
    // Delcaration
    int numbers[7];                         // Unintialised arrays
    int primes[5] = {2, 3, 5, 7, 11, 13};   // Initialised arrays
    int scores[5] = {10, 15};               // Partial initialization of arrays
    int temps[] = {36.5, 37.0, 38.7, 39.8}; // size reffered arrays
}

// An array is a collection of similar data items
// stored in contiguous memory locations.

/*
Arrays store multiple values of the same type.
*/